---
"@spon/payload-forms": patch
---

Update dependencies.

- **Runtime:** `papaparse` 5.5.3 → 5.5.4.
- **Dev tooling:** `@changesets/cli` 2.31.1, `@tailwindcss/postcss` / `tailwindcss` 4.3.3, `@tanstack/react-form` 1.33.3, `eslint` 9.39.5, `fallow` 2.104.0, `graphql` 16.14.2, `prettier` 3.9.6, `tsdown` 0.22.14.
- **pnpm:** added a `minimumReleaseAgeExclude` entry for `@rolldown/binding-android-arm64@1.2.2` so installs aren't blocked by the release-age gate.

All bumps are within existing semver ranges — no public API or behavioural changes.
